# server/.env.example
PORT=2567
# 기본값 0.0.0.0(모든 인터페이스, LAN 접속 가능) — 특정 인터페이스로 제한하려면 덮어쓴다
HOST=0.0.0.0

# Supabase — 로그인 유저의 칩 확인/차감/정산에 사용 (프로젝트 설정 > API 에서 확인)
SUPABASE_URL=
# ⚠️ service role 키 — RLS를 완전히 우회하는 키다. 절대 커밋/공유/클라이언트 코드에 넣지 말 것.
# 이 서버 프로세스 밖으로 나가면 안 된다.
SUPABASE_SERVICE_ROLE_KEY=