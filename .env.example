# .env.example — 개발 시 .env로 복사해 사용
VITE_SERVER_URL=ws://localhost:2567

# Supabase (로그인/칩 영속성) — 프로젝트 설정 > API 에서 확인
# anon key는 공개되어도 되는 키(RLS로 보호됨)지만, service role 키는 여기 넣지 말 것 (server/.env.example 참고)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
